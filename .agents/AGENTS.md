# Project Rules & Guidelines

## GitHub Push Constraints
- **ALWAYS ask for explicit user permission BEFORE executing any `git push` command.**
- Never automatically push commits to GitHub or remote repositories without the user's prior approval.
